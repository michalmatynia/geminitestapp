Cross-component tests that do not belong to a narrower component subfolder live here.

Prefer moving tests into a local component or subdomain folder when there is a clear owner. Keep this folder for shared harnesses and truly cross-cutting component specs.
