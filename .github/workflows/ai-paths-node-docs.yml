name: AI Paths Node Docs

on:
  pull_request:
    paths:
      - 'src/shared/lib/ai-paths/core/docs/**'
      - 'src/shared/lib/ai-paths/core/runtime/node-runtime-kernel.ts'
      - 'scripts/docs/**'
      - 'docs/ai-paths/node-code-objects-v2/**'
      - 'docs/ai-paths/node-code-objects-v3/**'
      - 'docs/ai-paths/semantic-grammar/nodes/**'
      - 'package.json'
      - 'package-lock.json'
  push:
    branches:
      - main
      - master
    paths:
      - 'src/shared/lib/ai-paths/core/docs/**'
      - 'src/shared/lib/ai-paths/core/runtime/node-runtime-kernel.ts'
      - 'scripts/docs/**'
      - 'docs/ai-paths/node-code-objects-v2/**'
      - 'docs/ai-paths/node-code-objects-v3/**'
      - 'docs/ai-paths/semantic-grammar/nodes/**'
      - 'package.json'
      - 'package-lock.json'

jobs:
  ai-paths-node-docs:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Verify AI Paths node docs artifacts
        run: npm run docs:ai-paths:node-docs:ci
