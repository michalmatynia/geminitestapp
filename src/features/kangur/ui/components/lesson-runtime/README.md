This folder owns the active lesson runtime surface: header, document renderer, lesson navigation, activity block rendering, and the shared lesson activity shell.

Keep lesson reading and in-lesson runtime components here. Catalog, intro, and lesson-browsing UI belongs in `lesson-library/`.
