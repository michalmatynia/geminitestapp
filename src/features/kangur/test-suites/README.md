## Kangur Test Suites

This folder owns Kangur test-suite metadata, question authoring helpers, question stores, and legacy test import utilities.

Keep tests-domain logic here instead of adding new loose files at `src/features/kangur`.
