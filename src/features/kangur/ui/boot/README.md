# Kangur UI Boot

This folder owns lightweight UI bootstrapping helpers used while the Kangur
frontend shell is initializing.

## Layout

- `boot-ready-signal.ts`: boot readiness signaling helpers for the UI shell
