This folder owns the Kangur game session runtime surfaces: navigation, question, result, XP toast, and the shared primitives they compose.

Keep play-session widgets and result/XP display primitives here with their local tests. Game setup flow belongs in `game-setup/`, quiz wrappers belong in `game-quiz/`, and higher-level page composition stays in `ui/pages/`.
