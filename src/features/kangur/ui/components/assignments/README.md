This folder owns the shared assignment-facing UI: priority chips, assignment lists, practice banners, spotlight surfaces, priority dashboard widgets, and learner-facing assignment panels.

Keep reusable assignment widgets here. Local tests belong in `assignments/__tests__/`. Assignment editing and authoring UI belongs in `assignment-manager/`.
