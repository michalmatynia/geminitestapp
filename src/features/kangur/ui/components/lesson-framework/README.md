This folder owns the reusable lesson-building framework: section hubs, slide-section shells, and the lesson helper utilities that wire them together.

Keep shared lesson structure here. Catalog browsing belongs in `lesson-library/`, and active in-lesson reading/runtime surfaces belong in `lesson-runtime/`.
