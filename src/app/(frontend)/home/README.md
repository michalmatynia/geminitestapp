Home/front-page route support for the public frontend.

This folder owns:
- front-page app selection and preview helpers
- home-page timing and normalization utilities
- home content rendering support
- home route tests

Keep the actual route entrypoints in `src/app/(frontend)` and `src/app/[locale]/(frontend)`.
