Cross-route layout tests for the app entrypoints.

This folder covers the shared layout contracts for:
- `src/app/layout.tsx`
- `src/app/[locale]/layout.tsx`
- `src/app/(admin)/layout.tsx`

The tests live here so route roots keep only actual layout entrypoints.
