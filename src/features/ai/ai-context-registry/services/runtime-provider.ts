import 'server-only';

import type {
  RuntimeContextProvider,
  RuntimeContextProviderResolveOptions,
} from '@/shared/contracts/ai-context-registry';

export type { RuntimeContextProvider, RuntimeContextProviderResolveOptions };
