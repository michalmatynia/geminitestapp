name: Bazel Quality

on:
  workflow_dispatch:
  pull_request:
    paths:
      - ".bazelignore"
      - ".bazelrc"
      - ".bazelversion"
      - ".github/workflows/bazel-quality.yml"
      - "BUILD.bazel"
      - "MODULE.bazel"
      - "REPO.bazel"
      - "docs/**"
      - "package-lock.json"
      - "package.json"
      - "scripts/architecture/**"
      - "scripts/canonical/**"
      - "scripts/docs/**"
      - "scripts/observability/**"
      - "scripts/perf/**"
      - "scripts/quality/**"
      - "scripts/testing/**"
      - "src/**"
      - "tools/bazel/**"
      - "tools/js/**"
  push:
    branches:
      - main
    paths:
      - ".bazelignore"
      - ".bazelrc"
      - ".bazelversion"
      - ".github/workflows/bazel-quality.yml"
      - "BUILD.bazel"
      - "MODULE.bazel"
      - "REPO.bazel"
      - "docs/**"
      - "package-lock.json"
      - "package.json"
      - "scripts/architecture/**"
      - "scripts/canonical/**"
      - "scripts/docs/**"
      - "scripts/observability/**"
      - "scripts/perf/**"
      - "scripts/quality/**"
      - "scripts/testing/**"
      - "src/**"
      - "tools/bazel/**"
      - "tools/js/**"

permissions:
  contents: read

jobs:
  bazel-quality:
    name: bazel-quality
    runs-on: ubuntu-latest
    timeout-minutes: 60
    env:
      BAZEL_REMOTE_CACHE_URL: ${{ secrets.BAZEL_REMOTE_CACHE_URL }}
      BAZEL_REMOTE_CACHE_HEADER: ${{ secrets.BAZEL_REMOTE_CACHE_HEADER }}
      BAZEL_REMOTE_CACHE_UPLOAD_LOCAL_RESULTS: ${{ github.event_name == 'push' && github.ref == 'refs/heads/main' && '1' || '0' }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Bazel repo quality lane
        run: npm run bazel:quality
