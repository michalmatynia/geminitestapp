Add Readability & maintainability Conventions - In Progress

12. Readability & maintainability conventions

Naming and file conventions

Consistent naming for server actions, validators, DTOs, hooks.

Small functions with single responsibility

Keep “God functions” out of route handlers.

Comments where they help - DONE

Prefer “why” comments (reasoning, invariants), not “what” comments.

Documentation for key flows

Short docs for: auth model, product lifecycle, DB constraints, deployment env vars.

A solid “next step” implementation order (high ROI)

Env validation + strict TS

Zod schemas at boundaries + consistent error model

Service/repository layering for Products

Structured logs + requestId + Sentry

Integration tests for Products service

CI + pre-commit gates + dependency scanning
