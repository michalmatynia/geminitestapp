import type {
  FolderTreeNodeDto,
  CreateFolderDto,
  UpdateFolderDto,
  MoveFolderDto,
  FolderTreeStatsDto,
} from '../../contracts/foldertree';

export type {
  FolderTreeNodeDto,
  CreateFolderDto,
  UpdateFolderDto,
  MoveFolderDto,
  FolderTreeStatsDto,
};

export type FolderTreeNode = FolderTreeNodeDto;

export type FolderTreeStats = FolderTreeStatsDto;
