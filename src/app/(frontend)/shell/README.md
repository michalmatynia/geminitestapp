Frontend shell support for the public route tree.

This folder owns shell-specific helpers and wrapper components used by the
public frontend layout, including timing instrumentation and the public-owner
shell wrapper. Shell tests should live in `shell/__tests__/`.
