Cross-route loading wrapper and loader inventory tests for the public frontend.

These tests intentionally live together because they verify shared loading-route
conventions across multiple frontend route families rather than one specific
route implementation.
