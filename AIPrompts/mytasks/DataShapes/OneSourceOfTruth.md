
I don't want to rely on inferred shapes from random code. 
Define domain types Objects (DTOs) explicitly.

API responses, server actions, and DB outputs should map to stable DTOs (don’t leak raw Prisma shapes into UI).
