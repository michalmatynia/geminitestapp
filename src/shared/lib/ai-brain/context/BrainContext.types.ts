/**
 * Brain Context Types
 * 
 * Type definitions for AI Brain context and UI.
 * Provides:
 * - Brain tab navigation types
 * - UI section identifiers
 * - Context state types
 * - Brain interface configuration
 * - Tab-based navigation structure
 */

export type BrainTab = 'operations' | 'routing' | 'providers' | 'reports' | 'metrics';
