This folder owns reusable Kangur logo and wordmark primitives.

Keep shared SVG/logo building blocks, localized wordmark factories, and their local `__tests__` here.
Keep family-specific wrappers with their owning feature only when they are tightly coupled to that feature's runtime.
