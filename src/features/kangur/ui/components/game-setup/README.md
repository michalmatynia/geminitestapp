This folder owns the Kangur game setup flow and its reusable setup widgets.

Keep the training setup, operation selector, setup-shell, momentum card, and local `__tests__` here.
Keep broader runtime/page orchestration outside this folder.
