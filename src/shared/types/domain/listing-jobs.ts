/**
 * Compatibility layer for listing jobs.
 * Unified types are now defined in src/shared/types/domain/integrations.ts
 */

export { 
  type ListingJob, 
  type ListingAttempt, 
  type ProductJob 
} from './integrations';
