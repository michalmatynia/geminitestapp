# Kangur Admin Context Registry

This folder owns admin context-registry bundle builders used to assemble
context-driven admin workspaces.

## Layout

- `lessons-manager.ts`: lessons-manager context bundle builder
- `__tests__/`: context-registry tests for this folder
